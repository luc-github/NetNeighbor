# NetNeighbor 2.0 (Qt / PySide6) — dépendances Python
# Usage: python -m pip install -r requirements-qt.txt
# (depuis un venv ; Python 3.10+)

-r requirements.txt
PySide6>=6.5.0
pywin32>=306; sys_platform == "win32"
